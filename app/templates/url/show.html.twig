{% extends 'base.html.twig' %}

{% block title %}
    View url
{% endblock %}

{% block body %}
    <h1>View url</h1>
    {% if url is defined and url|length %}
        <dl>
            <dt>Long Name</dt>
            <dd>{{ url.longName }}</dd>
            <dt>Short Name</dt>
            <dd>{{ url.shortName }}</dd>
            <dt>Create Time</dt>
            <dd>{{ url.createTime |date('Y-m-d H:i:s') }}</dd>
            <dt>Blocked</dt>
            <dd>{{ url.isBlocked ? 'Yes' : 'No' }}</dd>
            <dt>Blocked Till</dt>
            <dd>{{ url.blockTime |date('Y-m-d H:i:s') }}</dd>
        </dl>
        <p>
            <a href="{{ url('url_index') }}" title="{{ 'action.back_to_list'|trans }}">
                {{ 'action.back_to_list'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}